import { FormBackendActions } from './form-backend-actions';

describe('FormBackendActions', () => {
  it('should create an instance', () => {
    expect(new FormBackendActions()).toBeTruthy();
  });
});
