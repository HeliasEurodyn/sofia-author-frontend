import { FormAreaTranslationDTO } from './form-area-translation-dto';

describe('FormAreaTranslationDTO', () => {
  it('should create an instance', () => {
    expect(new FormAreaTranslationDTO()).toBeTruthy();
  });
});
