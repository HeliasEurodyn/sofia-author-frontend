import { RestDocumentationEndpointDTO } from './rest-documentation-endpoint-dto';

describe('RestDocumentationEndpointDTO', () => {
  it('should create an instance', () => {
    expect(new RestDocumentationEndpointDTO()).toBeTruthy();
  });
});
