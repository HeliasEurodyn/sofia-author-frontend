import { RemoveForeignKeyConstrainDTO } from './remove-foreign-key-constrain-dto';

describe('RemoveForeignKeyConstrainDTO', () => {
  it('should create an instance', () => {
    expect(new RemoveForeignKeyConstrainDTO()).toBeTruthy();
  });
});
