import { RestDocumentationDTO } from './rest-documentation-dto';

describe('RestDocumentationDto', () => {
  it('should create an instance', () => {
    expect(new RestDocumentationDTO()).toBeTruthy();
  });
});
