import {InternalMessageService} from './internal-message-service';

describe('InternalMessageService', () => {
  it('should create an instance', () => {
    expect(new InternalMessageService()).toBeTruthy();
  });
});
